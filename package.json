{
  "name": "prisma-issue-27442",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "migration:migrate": "npx prisma migrate dev --name",
    "prisma:reset:db": "npx prisma migrate reset",
    "migration:run": "npx prisma migrate deploy",
    "prisma:generate": "npx prisma generate",
    "prisma:format": "npx prisma format",
    "postinstall": "npm run prisma:generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "prisma": "^6.10.0"
  },
  "dependencies": {
    "@prisma/client": "^6.10.0"
  }
}
